# Token-Sentry — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- OpenAI SDK (Universal Provider) ---
openai>=1.0.0

# --- Token Counting (local, no API call needed) ---
tiktoken==0.7.0

# --- Async HTTP ---
httpx==0.27.2

# --- Redis (session + hot buffer) ---
redis==5.0.8

# --- Environment & Config ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Logging ---
structlog==24.4.0

# --- Vector Database ---
numpy<2.0.0
chromadb==0.5.0
sentence-transformers==2.7.0
